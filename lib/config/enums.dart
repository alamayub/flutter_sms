enum Role { admin, teacher, student, accountant, librarian, parent, staff }

enum AttendanceStatus { present, absent, leave }

enum AuthAction { login, register, authenticated }

enum MessageType { neutral, error, success }

enum StaffDesignation { teacher, accountant, staff }